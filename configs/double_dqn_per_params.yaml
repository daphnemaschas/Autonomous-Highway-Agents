experiment_name: "double_dqn_per_v1"

training:
  episodes: 5000

agent:
  lr: 0.0005
  gamma: 0.99
  buffer_capacity: 50000
  batch_size: 64
  target_update_freq: 500
  epsilon_start: 1.0
  epsilon_min: 0.05
  epsilon_decay: 0.99995

model:
  hidden_size: 256
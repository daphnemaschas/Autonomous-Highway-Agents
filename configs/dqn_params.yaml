experiment_name: "dqn_v1_big_buffer_2000_episodes"

training:
  episodes: 2000

agent:
  lr: 0.0005
  gamma: 0.99
  buffer_capacity: 50000
  batch_size: 64
  target_update_freq: 500
  epsilon_start: 1.0
  epsilon_min: 0.05
  epsilon_decay: 0.99995

model:
  hidden_size: 256